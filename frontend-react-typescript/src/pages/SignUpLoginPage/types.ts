export type LoginCardProps = {
    registerClick: () => void, 
    loginSuccess: (email: string) => void
}

export type SignupCardProps ={
    loginClick: () => void, 
    loginSuccess: (email: string) => void
}
